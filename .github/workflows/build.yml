name: Build and Patch LiveBridge APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Install Apktool
      run: |
        sudo wget https://raw.githubusercontent.com/iBotPeaches/Apktool/master/scripts/linux/apktool -O /usr/local/bin/apktool
        sudo chmod +x /usr/local/bin/apktool
        sudo wget https://github.com/iBotPeaches/Apktool/releases/download/v2.9.3/apktool_2.9.3.jar -O /usr/local/bin/apktool.jar
        sudo chmod +x /usr/local/bin/apktool.jar

    - name: Extract Project Files
      run: unzip LiveBridge-1.2.zip -d extracted_app/

    - name: Automatically Hex Edit libapp.so
      run: |
        python3 -c "
        import os
        
        # This will now search through every folder inside the extracted zip to find libapp.so automatically
        for root, dirs, files in os.walk('extracted_app'):
            if 'libapp.so' in files:
                file_path = os.path.join(root, 'libapp.so')
                print(f'Found and patching {file_path}...')
                
                with open(file_path, 'rb') as f:
                    data = f.read()
                
                # Replace the exact byte sequences with blank spaces
                data = data.replace(b'appsfolder.github.io/livebridge', b'                                 ')
                data = data.replace(b'github.com/appsfolder/livebridge', b'                                ')
                
                with open(file_path, 'wb') as f:
                    f.write(data)
        "

    - name: Rebuild APK
      run: |
        # This automatically finds the folder containing apktool.yml and builds it
        TARGET_DIR=$(dirname $(find extracted_app -name "apktool.yml" -type f | head -n 1))
        echo "Building from directory: $TARGET_DIR"
        apktool b "$TARGET_DIR" -o unsigned.apk

    - name: Sign APK
      run: |
        keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
        jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA256 -keystore debug.keystore -storepass android unsigned.apk androiddebugkey
        mv unsigned.apk final-debug.apk

    - name: Upload Finished APK
      uses: actions/upload-artifact@v4
      with:
        name: LiveBridge-Patched-APK
        path: final-debug.apk
        


